name: Demo
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print console
        run: echo Hello
      - name: Print a few lines
        run: | 
         echo first lines
         echo Second lines
